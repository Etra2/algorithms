package com.algorithms.graph;

//klasa reprezentująca węzeł grafu
class EdgeFB {
    int source, destination, weight;

    //konstruktor
    EdgeFB(int source, int destination, int weight) {
        this.source = source;
        this.destination = destination;
        this.weight = weight;
    }
}
